# Path to the credentials.json file downloaded from Google Cloud Console.
# Defaults to google/credentials.json if not set.
GOOGLE_CREDENTIALS_FILE=google/credentials.json

# Path where the OAuth token will be stored after the first login.
# Defaults to google/token.json if not set.
GOOGLE_TOKEN_FILE=google/token.json
